# TYPO3 Demo Environment
COMPOSE_PROJECT_NAME=typo3-demo

# Domain (used by Caddy for auto-HTTPS, use localhost for local dev)
TYPO3_DOMAIN=localhost
# Site base URL (used in TYPO3 site config; production: https://typo3-demo.netresearch.de/)
# TYPO3_SITE_BASE=/

# Database
MARIADB_ROOT_PASSWORD=CHANGE_ME
MARIADB_DATABASE=typo3
MARIADB_USER=typo3
MARIADB_PASSWORD=CHANGE_ME

# TYPO3
TYPO3_CONTEXT=Development/Docker
# TYPO3_ENCRYPTION_KEY=       # Auto-generated on first boot if empty; set for persistence across resets
# TYPO3_INSTALL_TOOL_PASSWORD=joh316  # Install tool password (default: joh316)

# OpenAI. Written here by `make persist-env-secret SECRET_NAME=OPENAI_API_KEY`
# during a deploy, so it survives a host reboot. Consumed by ai_filemetadata,
# which reads a plain key from its extension configuration and cannot use
# nr-vault. compose interpolates .env values, so a literal dollar is stored
# doubled ($$) here and arrives single in the container.
# OPENAI_API_KEY=

# Ports (override if host ports are taken)
# CADDY_HTTP_PORT=8080
# CADDY_HTTPS_PORT=8443

# Dev mode (make dev)
# DB_PORT=3306
# XDEBUG_MODE=debug,develop

# Hetzner (deploy only)
# HETZNER_SSH_HOST=
# HETZNER_SSH_USER=deploy
